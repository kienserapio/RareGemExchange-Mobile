/** Static app metadata and configuration. */
export const appConfig = {
  name: 'Rare Gem Exchange',
  tagline: 'Browse our curated collection of rare gems.',
  supportEmail: 'concierge@raregem.example',
  currency: 'USD',
} as const;
